import { Typography, Box } from '@mui/material';

const TeamsPage = () => {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        Teams
      </Typography>
      <Typography variant="body1">
        Teams management page - Coming soon
      </Typography>
    </Box>
  );
};

export default TeamsPage;
